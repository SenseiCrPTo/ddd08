import SwiftUI

struct TagSelectorView: View {
    var viewModel: TodoAddViewModel

    var body: some View {
        Section(header: Text("Метки")) {
            Text("Выбор меток будет реализован позже")
                .foregroundColor(.secondary)
        }
    }
}
